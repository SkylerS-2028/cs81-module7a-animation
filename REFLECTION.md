What part of the code was the most confusing and why? 
The most confusing part of the code for me was understanding why the code was using the modulo for the "frames[index % frames.length]" statement that assigned the frame to the loader. Intially, I would have thought to reset the index to zero at the start of each cycle, but after further thought I realized that this method is more convinient and requires less code.  

How does the animation help visualize asynchronous behavior?
It shows a visual representation of how functions can be carried out over time. 

What did you change or improve and what affect did it have? 
I added a second "..." frame and changed the status font color to gray while the animation was still loading. The additional frame made the animation smoother, while the change of the status font color added a further distinction between loading and the complete modes. Further, the gray provides a more incomplete visualization, furthering the graphic.
